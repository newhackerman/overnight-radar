[project]
name = "overnight-radar"
version = "0.1.0"
description = "Overnight US stock market radar with A-share impact mapping"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
target-version = "py311"
line-length = 120
